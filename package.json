{
  "scripts": {
    "start": "parcel index.html --no-source-maps",
    "build": "parcel build index.html -d dist --no-source-maps --public-url ./"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
